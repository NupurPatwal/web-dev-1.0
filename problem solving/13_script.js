/*Checking a Range:
Write a program to check if a number is within a range of numbers (e.g., 100 to 200).

*/
let num = 156;
if (num >= 100 && num <= 200) {
  console.log(`within range`);
} else {
  console.log(`not in range`);
}
