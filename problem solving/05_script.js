/*Logical AND Operator:
Write a program to check if a number is between 10 and 20, inclusive.

*/
let num = 119;
if (num >= 10 && num <= 20) {
  console.log(`yes num is is in between.`);
} else {
  console.log(`No the num is not in between.`);
}
